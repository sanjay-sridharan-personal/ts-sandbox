{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch App",
            "program": "${workspaceFolder}/index.ts",
            "args": [],
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}